
import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.jjsppl.pacificnwfishing',
  appName: 'Pacific NW Fishing Forecast',
  webDir: 'dist',
  ios: {
    contentInset: 'always'
  },
  android: {
  }
};

export default config;
